spring:
  datasource:
    url: jdbc:postgresql://my-postgres:5432/postgres
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true
server:
  port: 8080
